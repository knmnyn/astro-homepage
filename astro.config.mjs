/**
 * Astro configuration for the Min-Yen Kan homepage.
 *
 * This repo currently uses the default static Astro setup, so the config stays
 * intentionally minimal until we need adapters, integrations, or route-specific
 * build tweaks.
 */
// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({});
